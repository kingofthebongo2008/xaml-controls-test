<Page
    x:Class="App1.FinishPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <StackPanel>
        <Grid x:Name="LayoutRoot">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock x:Name="InputTextBlock1" Grid.Row="0" TextWrapping="Wrap" HorizontalAlignment="Left" Margin="0,0,0,15">
                When communicating with a web service that requires a single language to be specified,
            </TextBlock>
            
            <TextBlock x:Name="InputTextBlock2" Grid.Row="1" TextWrapping="Wrap" HorizontalAlignment="Left" Margin="0,0,0,15">
                This achieves appropriate language matching, consistent fallback across the user's
            </TextBlock>
            
            <TextBlock x:Name="InputTextBlock3" Grid.Row="2" TextWrapping="Wrap" HorizontalAlignment="Left">
                Click the button below to determine the url for the web service.
            </TextBlock>
            
            
        </Grid>
    </StackPanel>

</Page>
